apiVersion: v1
kind: service
metadata:
  name: backend
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - protocol: TCP
      name: http
      port: 8000
      targetPort: 8000